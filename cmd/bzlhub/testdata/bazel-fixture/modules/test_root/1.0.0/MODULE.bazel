"""Hermetic E2E fixture: root module, depends on test_mid → test_leaf."""
module(
    name = "test_root",
    version = "1.0.0",
    compatibility_level = 1,
)
bazel_dep(name = "test_mid", version = "1.0.0")
