// SPA mode: prerender = false, ssr = false. The Go server serves the static
// bundle and the BCR/api endpoints; the UI is fully client-side.
export const prerender = false;
export const ssr = false;
export const trailingSlash = 'never';
